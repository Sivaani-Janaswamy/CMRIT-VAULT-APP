import { PageShell } from "@/src/components/layout/page-shell";

export default function PyqsPage() {
  return (
    <PageShell title="PYQs" subtitle="Previous-year question papers integration starts in Phase C/D.">
      <div className="rounded-[var(--radius-card)] border border-[var(--border-soft)] bg-white p-4 text-sm text-[var(--muted)]">
        PYQ surface scaffold is ready.
      </div>
    </PageShell>
  );
}
