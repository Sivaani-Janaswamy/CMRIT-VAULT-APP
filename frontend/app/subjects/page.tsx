import { PageShell } from "@/src/components/layout/page-shell";

export default function SubjectsPage() {
  return (
    <PageShell title="Subjects" subtitle="Subjects listing will be connected to backend in Phase C.">
      <div className="rounded-[var(--radius-card)] border border-[var(--border-soft)] bg-white p-4 text-sm text-[var(--muted)]">
        Subject browsing scaffold is ready.
      </div>
    </PageShell>
  );
}
