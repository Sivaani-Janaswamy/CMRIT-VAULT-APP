import { PageShell } from "@/src/components/layout/page-shell";

export default function SearchPage() {
  return (
    <PageShell title="Search" subtitle="Search suggestions and result ranking will be wired in Phase D.">
      <div className="rounded-[var(--radius-card)] border border-[var(--border-soft)] bg-white p-4 text-sm text-[var(--muted)]">
        Search scaffold is ready.
      </div>
    </PageShell>
  );
}
